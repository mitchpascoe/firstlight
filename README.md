# Firstlight

A TUI for browsing ESA/Webb telescope images and setting them as your macOS wallpaper.

## Setup

```
cargo build --release
```

## Usage

```
cargo run --release
```

- `j`/`k` — navigate
- `enter` — set wallpaper (all desktops)
- `/` — filter by title
- `q` — quit
